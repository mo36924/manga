[project]
name = "manga"
version = "0.0.0"
requires-python = ">=3.14,<3.15"

[tool.uv]
package = false
link-mode = "symlink"
add-bounds = "major"
exclude-newer = "3 days"

[dependency-groups]
dev = ["ruff>=0.16.8,<0.17.0"]
